# Copiá este archivo a deploy.env en la raíz del repo en la VPS:
#   cp scripts/deploy.env.example deploy.env
#
# Solo hace falta si el clone del repo NO está en /var/www/avergas.

# Carpeta donde Nginx sirve la página
WEB_ROOT=/var/www/avergas

# Rama a desplegar
GIT_BRANCH=main
